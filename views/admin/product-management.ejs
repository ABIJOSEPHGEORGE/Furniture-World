<%- include('includes/admin/head') %>
<%- include('includes/admin/header') %>
  
    <section class="container w-100 mt-3 mx-0 p-0">
        <!-- <form action="/admin/coupon-management" method="post" class="d-flex flex-column justify-content-center align-items-center">
            <div class="border-bottom d-flex flex-row align-items-center col-10 bg-dark rounded-pill px-3">
              <span class="material-symbols-outlined col-1">search</span>
              <input type="search" class="col-9 bg-transparent border-0 text-white" placeholder="Search coupon" name="search_key">
              <button class="btn col-2" type="submit">Search</button>
            </div>
          </form> -->

         <% if(products.length!=0){%>
            <table class="table table-striped table-dark">
                <thead>
                  <tr>
                    <th scope="col">SL. NO</th>
                    <th scope="col">Product Image</th>
                    <th scope="col">Product Name</th>
                    <th scope="col">Category</th>
                    <th scope="col">Stock</th>
                    <th scope="col">Status</th>
                    <th scope="col">Sale Price</th>
                    <th scope="col" class="text-center">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% products.forEach((product,index)=>{%>
                  <tr>
                    <td scope="row"><%-index+1%></td>
                    <td class="w-25">
                      <img src="<%-product.images[0].URL%>" alt="" srcset="" class="w-50">
                    </td>
                    <td><%-product.product_name%></td>
                    <td><%-product.parent_category[0].category_name%></td>

                    <% if(product.stock>0){ %>
                        <td><span class="badge badge-pill bg-success px-2 rounded-pill">In stock</span></td>
                        <%}else{%>
                        <td><span class="badge badge-pill bg-danger px-2 rounded-pill">Out of stock</span></td>
                        <%}%>

                    <% if(product.status===true){ %>
                    <td><span class="badge badge-pill bg-success px-2 rounded-pill">Listed</span></td>
                    <%}else{%>
                    <td><span class="badge badge-pill bg-danger px-2 rounded-pill">Unlisted</span></td>
                    <%}%>
                    <td><%-product.sale_price%></td>
                    <td >
                        <div class="d-flex flex-row justify-content-evenly align-items-center">
                            
                          <div>
                            <a href="view-product?id=<%-product._id%>" class="d-flex flex-column justify-content-center align-items-center px-2">
                                <span class="material-symbols-outlined text-white">visibility</span>
                                <p class="text-white">View</p>
                            </a>
                          </div>
                          <% if(product.status==true){%>
                          <div>
                            <a href="/admin/product-status/<%-product._id%>/?status=false&_method=PUT" class="d-flex flex-column justify-content-center align-items-center px-2">
                                <span class="material-symbols-outlined text-danger">toggle_off</span>
                                <p class="text-white">Unlist</p>
                            </a>
                          </div>
                          <%}else{%>
                            <div>
                                <a href="/admin/product-status/<%-product._id%>/?status=true&_method=PUT" class="d-flex flex-column justify-content-center align-items-center px-2">
                                    <span class="material-symbols-outlined text-success">toggle_on</span>
                                    <p class="text-white">list</p>
                                </a>
                              </div>
                            <%}%>
                        </div>
                    </td>
                  </tr>
                  <% }) %>
                </tbody>
              </table>
              <% } %>
              
              <% if(products.length==0){ %>
              <div class="w-100 d-flex flex-column justify-content-center align-items-center">
                <img src="/admin/img/Empty-bro.svg" class="w-25 h-25" alt="">
                <p class="text-white">No products found</p>
                <a href="/admin/add-product" class="text-white text-decoration-underline">Add new product</a>
              </div>
             <%}%>

    </section>

<%- include('includes/admin/footer') %>